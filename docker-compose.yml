# version: '2'

# services:
#   productdetails:
#     build:
#       context: .
#       dockerfile: Dockerfile
#     container_name: productdetails
#     ports:
#       - "8080:3001"

version: '3'

services:
  maze:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mazeapp
    ports:
      - "3001:3001"